@model IEnumerable<RoleVM>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>All Users</h1>
<br />
<br />

<a asp-action="Create" class="btn btn-primary">Create Role</a>

<form asp-action="Index">
  <div class="row m-5">
        <div class="col-8">
            <input type="text" name="InputSearch" class="form-control" />
        </div>
        <div class="col-4">
            <input type="submit" class="btn btn-success" />
        </div>
  </div>
</form>
 

@if (Model.Count() != 0)
{
    <table  class="table table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(e=>e.Id)</th>
                <th>@Html.DisplayNameFor(e=>e.Name)</th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var role in Model)
            {
            <tr>
                    <td>@role.Id</td>
                    <td>@role.Name</td>
                    <td><a asp-action="Details" asp-route-id="@role.Id" class="btn btn-warning">Details</a></td>
                    <td><a asp-action="Edit" asp-route-id="@role.Id" class="btn btn-success">Edit</a></td>
                    <td><a asp-action="Delete" asp-route-id="@role.Id" class="btn btn-danger">Delete</a></td>
            </tr>
            }
           
        </tbody>
    </table>
  
}
else
{
 <div class="alert alert-danger text-center">
     <h5>No Role Found</h5>
 </div>   
}

